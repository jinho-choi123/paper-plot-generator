# Examples

Run these commands from the repository root.

```bash
uv run paper-plot examples/grouped_bar.yaml
uv run paper-plot examples/stacked_bar.yaml
uv run paper-plot examples/line_series.yaml
```

Each command writes a PDF and PNG under `examples/figures/`.

